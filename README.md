📋 Требования (Requirements)
Node.js: v21 или выше

🚀 Установка и запуск (Installation & Setup)
Bash

# Установка зависимостей
npm install

# Запуск сервера разработки
npm run dev
📂 Структура проекта (Project Structure)
Plaintext

src/
├── api/                     # Слой взаимодействия с бэкендом (Request builders)
│   ├── appeals.js           
│   └── index.js             
│   
├── components/              
│   ├── ui/                  # Переиспользуемые UI-компоненты (Atomic components)
│   │   ├── Button/
│   │   ├── Card/
│   │   ├── SearchInput/
│   │   ├── StatCard/
│   │   └── StatusBadge/
│   │
│   ├── layout/              # Компоненты разметки страниц
│   │   ├── Header/
│   │   ├── Sidebar/
│   │   ├── Footer/
│   │   └── MainLayout/
│   │
│   └── features/            # Крупные бизнес-компоненты
│       ├── AppealsTable/
│       ├── AppealsMap/
│       └── AppealModal/
│
├── hooks/                   # Кастомные React хуки
│   └── useAppeals.js
│
├── pages/                   # Страницы приложения (Route views)
│   ├── DashboardPage/
│   ├── TablePage/
│   ├── MapPage/
│   ├── StatisticsPage/
│   └── ReportsPage/
│
├── data/                    # Статические данные (Mock data)
│   └── data.json
│
├── App.jsx
└── main.jsx
💡 Архитектурные заметки (Architecture Notes)
API Layer (src/api/):

В текущей версии используется базовый билдер запросов.

При подключении реального API: * Необходимо создать api.base.js для конфигурации axios (Base URL + Interceptors).

Интерфейсы и типы данных должны быть вынесены в отдельный файл types.js.
